
@model List<ProfessorModel>

<div class="mb-5 d-flex justify-content-between align-items-center mt-5">
    <h1>Listagem de professores</h1>
    <a class="btn btn-success" asp-action="CadastrarProfessor" asp-controller="Professor">Criar Professor</a>
    
</div>


<table class="table table-striped table-bordered" id="registros">
    <thead class="table-primary">
        <tr>
            <td scope="col">Id</td>
            <td scope="col">Email</td>
            <td scope="col">Matéria</td>
            <td scope="col"></td>
            
        </tr>
    </thead>

    <tbody>
        @foreach(var professor in Model)
        {
            <tr>
                <th scope="row">@professor.Id</th>
                <td>@professor.Nome</td>
                <td>@professor.Email</td>
                <td>@professor.Materia.Descricao</td>
                <td width="100px" class="text-center">
                    <a asp-action="DetalhesProfessor" asp-controller="Professor" asp-route-id="@professor.Id"> Detalhes</a>
                </td>
            </tr>
        } 
    </tbody>
</table>